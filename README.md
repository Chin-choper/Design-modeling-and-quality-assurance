Відео ЛБ1 - https://drive.google.com/drive/folders/1iKVqhS0wOIJpgbSg0cogQdmi8mbK7iNj?usp=sharing
Відео ЛБ2 - https://drive.google.com/drive/folders/1mEYtNKmLjy2ZU9WQdAgTlxASh8DyaBol?usp=sharing
Відео ЛБ3 - https://drive.google.com/drive/folders/17idhaNV8khWRd707a5HhV8APbD6T5xcb?usp=sharing
Відео ЛБ4 -
Відео ЛБ5 -

📂 1. Структура та головні файли

### Кореневі файли
* `manage.py`: Командний центр Django. [cite_start]Виконує запуск сервера, міграції, створення суперюзера та управління БД.
* `requirements.txt`: Список бібліотек Python. [cite_start]Використовується для налаштування оточення командою `pip install -r requirements.txt`.
* [cite_start]`uwsgi.ini`: Конфігурація для сервера uWSGI (запуск 4-х процесів, шлях до віртуального оточення).
* [cite_start]`db.sqlite3`: Локальна SQL база даних для зберігання користувачів та моделей Django.

### Дані
* `test_data` / `xlsx files`: Файли з даними про експорт та імпорт. [cite_start]Принцип роботи: скрипт зчитує дані (з txt або Excel) та наповнює контекст для відображення аналітики.

---

## ⚙️ 2. Логіка додатку (folder `operations/`)

### Робота з базою даних (Hybrid: SQL + MongoDB)
Логіку розширено для підтримки NoSQL рішення.

* **`operations/utils.py`**: Відповідає за з'єднання з MongoDB. [cite_start]Використовує `pymongo` для створення `MongoClient` та доступу до колекції.
* **`operations/models.py`**: Класичні Django ORM моделі (SQL) для зберігання структурованих даних операцій.

### Views (`operations/views.py`)
* **Analytics (SQL/Pandas):** Обробка Excel-файлів, розрахунок статистики експорту/імпорту та побудова графіків.
* **MongoDB CRUD:**
    * [cite_start]`mongo_list`: Отримує всі документи (`find`).
    * [cite_start]`mongo_create`: Створення нових документів (`insert_one`).
    * [cite_start]`mongo_edit`: Пошук за `ObjectId` та оновлення (`update_one`).
    * [cite_start]`mongo_delete`: Видалення документу (`delete_one`).

---

## 🧪 3. Тестування (New)

У проект додано модульні тести (`unit tests`) для перевірки надійності системи.

* **Файл:** `operations/tests.py`
* **Функціонал:**
    * Тестування моделей Django (створення об'єктів, перевірка полів).
    * Тестування HTTP-відповідей (status 200, used templates).
    * **Mocking:** Імітація підключення до MongoDB для тестування функцій створення/редагування без запису в реальну базу даних.
* **Запуск тестів:**
    ```bash
    ./manage.py test
    ```

---

## 🎨 4. Інтерфейс (folder `templates/`)

[cite_start]Додано шаблони для CRUD-операцій з MongoDB:
* `operation_list.html`: Головна сторінка з аналітикою.
* `mongo_list.html`: Таблиця даних з MongoDB + кнопки управління.
* `mongo_create.html`: Форма додавання (Підприємство, Тип, Товар, Сума).
* `mongo_edit.html`: Форма редагування існуючих записів.

---

## 🚀 5. Встановлення та запуск (WSL/Linux)

Для роботи потрібен WSL Linux, Python 3 та встановлений MongoDB.
### 1. Налаштування оточення
Перейдіть у директорію проекту та активуйте віртуальне оточення:
```bash
source ~/lb1/bin/activate
cd ~/djangosite

---

## 📦 6. Встановлення залежностей

pip install -r requirements.txt

---

## ⚙️ 7. Запуск через Supervisor

Проект налаштовано для роботи у фоновому режимі через файл /etc/supervisor/conf.d/djangosite.conf. Запуск:
sudo supervisorctl start djangosite
Запуск сторінки з MongoBD:
sudo service mongod start

---

## 🌐 8. Доступ до сайту

Дізнайтеся вашу IP-адресу в локальній мережі:
hostname -I

Відкрийте у браузері: Підставте отриману IP-адресу у посилання:
http://<ВАШ_IP_ADRESS>:9000/
Для сторінки з MongoBD:
http://<ВАШ_IP_ADRESS>:9000/mongo/

